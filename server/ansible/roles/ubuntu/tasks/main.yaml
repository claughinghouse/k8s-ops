---

- include: locale.yaml
  tags:
  - locale

- include: packages.yaml
  tags:
  - packages

- include: kernel.yaml
  tags:
  - kernel

- include: network.yaml
  tags:
  - network

- include: filesystem.yaml
  tags:
  - filesystem

- include: unattended-upgrades.yaml
  tags:
  - unattended-upgrades

- include: user.yaml
  tags:
  - user
